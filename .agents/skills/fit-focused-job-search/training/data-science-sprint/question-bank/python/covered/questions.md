# Covered Python — Verified Practice History

## Python foundations

Practiced:
- loop + condition;
- append filtered values;
- count zeros / counter syntax;
- filter non-zero values;
- move zeros to end;
- count values;
- largest without `max()`;
- smallest without `min()`;
- sum without `sum()`;
- conditional sum;
- average without `sum()`.

Recurring gaps:
- `+= 1` counter syntax;
- combining multiple simple loop patterns;
- running-best pattern;
- running-total accumulation;
- output placement outside loops.

Stronger areas:
- basic filtering;
- simple loop conditions;
- smallest / conditional sum after practice.
