# Future Python / pandas Practice Bank

Use after the SQL mastery phase or for scheduled maintenance.

## Raw Python sequence

1. **PY-01** — Counter retry: count values matching a condition.
2. **PY-02** — Running total retry: sum selected values without `sum()`.
3. **PY-03** — Running best retry: largest value without `max()`.
4. **PY-04** — Combine filter + counter + reconstruction.
5. **PY-05** — Frequency map with dictionary.
6. **PY-06** — First non-repeating value.
7. **PY-07** — Group-like aggregation using dictionaries.
8. **PY-08** — Sliding-window sum.
9. **PY-09** — Sliding-window maximum (basic version).
10. **PY-10** — Explain time and space complexity.

## pandas sequence

1. **PD-01** — Filter rows with multiple conditions.
2. **PD-02** — Create calculated column with missing values.
3. **PD-03** — `groupby().agg()` with multiple metrics.
4. **PD-04** — Merge two DataFrames with incomplete match coverage.
5. **PD-05** — Identify unmatched rows after left merge.
6. **PD-06** — Fill or preserve missing values intentionally.
7. **PD-07** — Rank within groups.
8. **PD-08** — Previous-row metric within group.
9. **PD-09** — Reusable metric function.
10. **PD-10** — Debug a grain/duplicate-row error after merge.

Default: one question at a time; do not show answer first.
