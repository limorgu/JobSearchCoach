# Future Python / pandas Practice Bank

Use after the SQL mastery phase or for scheduled maintenance.

## Raw Python sequence

1. Counter retry — count values matching a condition.
2. Running total retry — sum selected values without `sum()`.
3. Running best retry — largest value without `max()`.
4. Combine filter + counter + reconstruction.
5. Frequency map with dictionary.
6. First non-repeating value.
7. Group-like aggregation using dictionaries.
8. Sliding-window sum.
9. Sliding-window maximum (basic version).
10. Explain time and space complexity.

## pandas sequence

1. Filter rows with multiple conditions.
2. Create calculated column with missing values.
3. `groupby().agg()` with multiple metrics.
4. Merge two DataFrames with incomplete match coverage.
5. Identify unmatched rows after left merge.
6. Fill or preserve missing values intentionally.
7. Rank within groups.
8. Previous-row metric within group.
9. Reusable metric function.
10. Debug a grain/duplicate-row error after merge.

Default: one question at a time; do not show answer first.
