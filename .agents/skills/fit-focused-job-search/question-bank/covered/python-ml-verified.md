# Covered Python / ML — Verified Practice History

## Python foundations

Practiced:
- loop + condition;
- append filtered values;
- count zeros / counter syntax;
- filter non-zero values;
- move zeros to end;
- count values;
- largest without `max()`;
- smallest without `min()`;
- sum without `sum()`;
- conditional sum;
- average without `sum()`.

Recurring gaps:
- `+= 1` counter syntax;
- combining multiple simple loop patterns;
- running-best pattern;
- running-total accumulation;
- output placement outside loops.

Stronger areas:
- basic filtering;
- simple loop conditions;
- smallest / conditional sum after practice.

## ML foundations

Practiced:
- supervised vs unsupervised;
- self-supervised distinction;
- overfitting detection;
- overfitting remedies;
- train / validation / test;
- validation vs cross-validation.

Recurring gaps:
- unsupervised vs self-supervised;
- diagnostic technique vs actual overfitting remedy;
- validation set purpose vs CV folds.

## ML / evaluation cases

Practiced:
- target definition for escalation classification;
- recall vs precision;
- calculate recall/precision from confusion-matrix counts;
- guardrails when optimizing recall;
- false-negative vs false-positive cost;
- threshold lowering effects;
- cost-sensitive threshold choice;
- diagnosing high recall with rising serious complaints;
- subgroup launch decision;
- improving subgroup recall with rare severe cases;
- oversampling vs production evaluation distribution.

Stronger areas:
- recall/precision formulas after correction;
- threshold trade-offs;
- incident diagnosis;
- cost-sensitive reasoning.

Recurring gap:
- aggregate metrics must not hide weak subgroup performance.
